---
title: "CS 125 Course Staff"
description:
  Your journey in computer science is beginning. We're here to help you along
  the way.
priority: 0.9
changefreq: monthly
layout: single.hbt
---
<div class="mt-3 spelling_exception">
  <h1 class="text-primary">Instructors</h1>
  <div class="row">
    {{#each course.roles.Instructor }}
      <div class="col-12 col-sm-6 col-md-4 mt-2">
        <div class="card pt-2 pl-2 pr-2">
          <img class="card-img-top" src="{{{ this.photo }}}" alt="Photo of {{{ this.name }}}">
          <div class="card-body">
            <h4 class="card-title text-center">{{{ this.name }}}</h4>
              <p class="card-text">
              Teaching lectures{{#if this.sections}}
                and {{ inflect this.sections.length "section" "sections" }}
                {{ and_list this.sections }}.
                {{else}}.{{/if}}
              </p>
          </div>
        </div>
      </div>
    {{/each }}
  </div>
</div>

<div class="mt-3 spelling_exception">
  <h1 class="text-primary">Teaching Assistants</h1>
  <div class="row">
    {{#each course.roles.TA }}
      <div class="col-12 col-sm-4 col-md-3 mt-2">
        <div class="card pt-2 pl-2 pr-2">
          <img class="card-img-top" src="{{{ this.photo }}}" alt="Photo of {{{ this.name }}}">
          <div class="card-body">
            <h4 class="card-title text-center">{{{ this.name }}}</h4>
              {{#if this.sections }}
                <p class="card-text">
                  Teaching {{ inflect this.sections.length "section" "sections" }}
                  {{ and_list this.sections }}.
                </p>
              {{/if}}
          </div>
        </div>
      </div>
    {{/each }}
  </div>
</div>

<div class="mt-3 spelling_exception">
  <h1 class="text-primary">Illinoisan Doyens</h1>
  <div class="row">
    {{#each course.roles.Doyen }}
      <div class="col-12 col-sm-4 col-md-3 mt-2">
        <div class="card pt-2 pl-2 pr-2">
          <img class="card-img-top" src="{{{ this.photo }}}" alt="Photo of {{{ this.name }}}">
          <div class="card-body">
            <h4 class="card-title text-center">{{{ this.name }}}</h4>
              {{#if this.sections }}
                <p class="card-text">
                  Assisting with {{ inflect this.sections.length "section" "sections" }}
                  {{ and_list this.sections }}.
                </p>
              {{/if}}
          </div>
        </div>
      </div>
    {{/each }}
  </div>
</div>
