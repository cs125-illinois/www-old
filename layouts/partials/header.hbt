<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    {{#each metadata.webpack.assetsByType['css'] }}
    <link rel="stylesheet" href="{{ this }}" >
    {{/each}}

    {{#each metadata.webpack.assetsByType['js'] }}
    <script src="{{ this }}" async></script>
    {{/each}}
  </head>

<!--
  vim: et:ts=2:sw=2:ft=html
-->
